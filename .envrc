# direnv + devbox: entering this directory puts uv, yarn, task and python on PATH.
# Requires direnv and devbox. Without direnv, use `devbox shell` instead.
eval "$(devbox generate direnv --print-envrc)"
